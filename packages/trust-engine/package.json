{
  "name": "@zuzz/trust-engine",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'trust-engine lint ok'",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@zuzz/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
